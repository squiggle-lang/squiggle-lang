let (
    _printP = ~{
        "pre": ~( x | x > 0 )
    }( x |
        print(x)
    ),
    _foo = ~( x | print(x) ),
    _log = global.console::log
) in [
    true,
    false,
    null,
    undefined
]
